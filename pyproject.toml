[project]
name = "design_studio"
authors = [
    { name = "Beetashoke Chakraborty", email = "beetashokechakraborty721@gmail.com"}
]
description = "Design studio"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=15.0.0" # Installed and managed by bench.
    "anthropic~=0.116",
    # Pinned to 1.x: 2.x pulls in google-auth>=2.48.1, which conflicts with
    # frappe's own google-auth~=2.48.0 pin (2.48.1 doesn't exist as a release).
    "google-genai~=1.39",
    # Unpinned: frappe core already depends on a compatible Pillow version.
    "Pillow",
    # Handles Hugging Face Inference Providers routing (provider="auto") so image
    # generation keeps working as their free-tier provider catalog changes.
    "huggingface_hub[inference]>=0.30",
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"

# These apt dependencies will be installed from Ubuntu repositories when you host your app on Frappe Cloud
[deploy.dependencies.apt]
packages = []

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = [
    "F",
    "E",
    "W",
    "I",
    "UP",
    "B",
    "RUF",
]
ignore = [
    "B017", # assertRaises(Exception) - should be more specific
    "B018", # useless expression, not assigned to anything
    "B023", # function doesn't bind loop variable - will have last iteration's value
    "B904", # raise inside except without from
    "E101", # indentation contains mixed spaces and tabs
    "E402", # module level import not at top of file
    "E501", # line too long
    "E741", # ambiguous variable name
    "F401", # "unused" imports
    "F403", # can't detect undefined names from * import
    "F405", # can't detect undefined names from * import
    "F722", # syntax error in forward type annotation
    "W191", # indentation contains tabs
]
typing-modules = ["frappe.types.DF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
docstring-code-format = true
